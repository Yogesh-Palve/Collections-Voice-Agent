# Web framework and ASGI server
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
websockets>=13.0,<15.0
python-multipart>=0.0.12

# Configuration and validation
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# LLM
groq>=0.11.0,<1.0.0

# STT
deepgram-sdk>=3.7.0,<5.0.0
elevenlabs>=1.0.0,<2.0.0

# TTS / HTTP clients
httpx>=0.27.0,<1.0.0

# Audio processing (setuptools must be installed before webrtcvad on Python 3.12+)
setuptools>=70.0.0
webrtcvad>=2.0.10
numpy>=1.26.0,<3.0.0
scipy>=1.14.0,<2.0.0

# Telephony
twilio>=9.3.0,<10.0.0

# Data persistence
openpyxl>=3.1.0,<4.0.0

# Demo UI
streamlit>=1.39.0,<2.0.0

# Optional local microphone (Streamlit / CLI tests on desktop)
# pyaudio may require system PortAudio; comment out if install fails on CI
pyaudio>=0.2.14; platform_system != "Linux" or platform_machine != "aarch64"

# Dev / testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
